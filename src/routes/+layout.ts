import {Theme} from "../lib/Theme";

export const load = async () => {

    return {
        theme: {
            name: "Forresty",
            primary: "black-pearl",
            secondary: "malibu",
            neutral: "shuttle-gray"
        }
    }
}